name: c1

on: 
  push:
    branches:
      - main
      - person1
      - person2

jobs:
  build-main:
    runs-on: windows-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install dependencies
      - name: Build project
        run: Cryptograms.py
        
  build-person1:
    runs-on: windows
    if: github.ref == 'refs/heads/person1'
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install dependencies
        run: npm install
      - name: Build project
        run: npm run build
        
  build-person2:
    runs-on: windows
    if: github.ref == 'refs/heads/person2'
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install dependencies
        run: npm install
      - name: Build project
        run: npm run build
        
     
    
    
        
